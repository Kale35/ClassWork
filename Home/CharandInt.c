#include <stdio.h>

int main()
{
    //definition and initializing d and f

    int a = 3, b = 5, c;

    c = a + b;

    //Variable x has the value of E

    char x = 'E';
    char y = 70;

    //ASCII code
    printf("x = %c, x = %d\n", x, x);
    printf("y = %c, y = %d\n", y, y);
    printf('value of c : %d \n', c);

    return (0);
}